{
  "private": true,
  "name": "texy",
  "scripts": {
    "dev": "vbuild -dc",
    "build": "vbuild -c",
    "gh": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "clipboard": "^1.5.15",
    "lodash.throttle": "^4.1.1",
    "native-toast": "^0.2.0",
    "query-string": "^4.2.3",
    "taboverride": "^4.0.3"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "postcss-nested": "^1.0.0"
  }
}
